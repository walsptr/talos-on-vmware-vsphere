apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "superset-test.fullname" . }}-test-connection"
  labels:
    {{- include "superset-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "superset-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
