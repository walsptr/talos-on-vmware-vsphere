apiVersion: v1
kind: Service
metadata:
  name: {{ include "superset-test.fullname" . }}
  labels:
    {{- include "superset-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "superset-test.selectorLabels" . | nindent 4 }}
